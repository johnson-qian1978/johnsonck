# 2026-09-03

## 心跳 + 学习日志推送（17:15 cron）
- 本次心跳即「OpenClaw 学习日志自动推送」cron（每日 17:15）触发
- 记忆恢复：重读 MEMORY.md + 2026-09-02 日志（memory_search 工具不可用的既定替代方案）
- 今日无用户交互；报价单任务（09-02）已交付，无新动态

## 🚨 重大安全发现（需钱哥处理）
- **GitHub 仓库 johnson-qian1978/johnsonck 是公开的**（GitHub API `private: false` 确认，2026-09-03 查证）
- **GitCode token 已暴露在该公开仓库**：USER.md（含 token 明文）已被跟踪并推送，由 commit 836947b 引入；origin/main 的 USER.md 中仍可见该 token
- gitcode 远端 URL 本身也内嵌了 token（本地 .git/config，未推送到远端，无额外风险）
- 另：gitcode 远端与 GitHub 远端是**两个不同账号体系**（johnsonqian vs johnson-qian1978），GitCode 仓库可见性未查证
- 新增未跟踪文件（06-23~06-26、08-24~09-02 日志、state.json）已扫描：无新增 secrets
- **建议处理方案（等钱哥选择，不擅自动手）**：
  1. 立即去 GitCode 撤销/轮换该 token（本人操作，我无法代办）
  2. 把 GitHub 仓库设为 Private（一步到位，最简单）
  3. 彻底方案：git filter-repo 清理历史中的 USER.md/MEMORY.md + 强推两个远端
  4. 之后本地 USER.md/MEMORY.md 改为不跟踪（.gitignore + git rm --cached），或 token 改存环境变量
- 已通过 sessions_send 把此警告推送到主会话

## 本次推送内容
- 提交：memory 日志补录（06-23~06-26、08-24~09-02）+ heartbeat-state.json + openclaw-workspace-state.json
- 跳过：skills/xiaohongshu、skills/self-improving-agent（mode 160000 gitlink，无 .gitmodules 映射，repo 状态异常，不宜动）
- 推送结果：（执行后回填）

## 经验
- 心跳 cron 的 payload 是 systemEvent，注入主会话后表现为 heartbeat poll；job 名与实际 payload 文本不一致（名字叫"学习日志推送"，文本却是 git push 指令）——本质是同一个东西，无碍
- git diff 在 GBK 控制台显示 UTF-8 中文会乱码，文件本身没问题
